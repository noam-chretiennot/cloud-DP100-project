name: myenv
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - azureml-core
    - azureml-dataprep[pandas]
    - azureml-defaults
    - scikit-learn
    - pandas
    - numpy
    - joblib